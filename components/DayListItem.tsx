import React from "react";

const DayListItem = ({
  name,
  spots,
  selected,
  setDay,
}: {
  name: string;
  spots: number;
  selected: boolean;
  setDay: any;
}) => {
  return <div>DayListItem</div>;
};

export default DayListItem;
